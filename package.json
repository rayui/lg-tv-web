{
  "dependencies": {
    "dotenv": "^16.0.3"
  },
  "scripts": {
    "start": "yarn workspaces run start",
    "build": "yarn workspaces run build"
  },
  "private": true,
  "workspaces": [
    "packages/http2serial",
    "packages/client"
  ]
}
